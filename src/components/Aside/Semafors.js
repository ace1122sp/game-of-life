import React from 'react';

const Semafors = ({generation}) => {
  return(
    <div className="semafors">
      <label key="0">generation: {generation}</label>
    </div>
  );
}

export default Semafors;
